# ============================================================================
# ENVIRONMENT CONFIGURATION EXAMPLE
# Copy this file to .env and fill in your actual values
# ============================================================================

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/listening_agent  # For production with PostgreSQL
# Leave empty for local development (SQLite will be used automatically)

# JWT Secret Key (Generate a secure random string for production)
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# Google OAuth (for Google Sign-In)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth (for GitHub Sign-In)
GITHUB_CLIENT_ID=your-github-oauth-app-client-id
GITHUB_CLIENT_SECRET=your-github-oauth-app-client-secret

# Frontend URL (for OAuth callbacks)
FRONTEND_URL=http://localhost:3000

# AssemblyAI (for transcription and diarization)
ASSEMBLYAI_API_KEY=your-assemblyai-api-key

# Google Gemini (for AI extraction)
GEMINI_API_KEY=your-google-gemini-api-key

# LLM Provider Selection
LLM_PROVIDER=gemini  # Options: gemini, groq
LLM_MODEL_NAME=gemini-2.0-flash-exp  # or models/gemini-2.0-flash-exp

# Optional: Groq (backup LLM provider)
# GROQ_API_KEY=your-groq-api-key

# Email / SMTP Configuration (for password reset and email verification)
# Required for production, optional for development
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=your-email@gmail.com
SMTP_FROM_NAME=ESAPListen

# Leave SMTP_USERNAME and SMTP_PASSWORD empty for development mode
# In development, reset/verification URLs will be logged to console instead of emailed

# ============================================================================
# HOW TO GET API KEYS
# ============================================================================

# Google OAuth:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Google+ API
# 4. Go to Credentials → Create Credentials → OAuth Client ID
# 5. Select "Web application"
# 6. Add authorized redirect URIs: http://localhost:3000/signin
# 7. For calendar access, also add calendar scopes in OAuth consent screen

# GitHub OAuth:
# 1. Go to https://github.com/settings/developers
# 2. Click "New OAuth App"
# 3. Fill in:
#    - Application name: Listening Agent
#    - Homepage URL: http://localhost:3000
#    - Authorization callback URL: http://localhost:3000/signin
# 4. Copy Client ID and generate a Client Secret

# AssemblyAI:
# 1. Go to https://www.assemblyai.com/
# 2. Sign up and get your API key from the dashboard

# Google Gemini:
# 1. Go to https://ai.google.dev/
# 2. Get API key from Google AI Studio

# Groq (Optional):
# 1. Go to https://console.groq.com/
# 2. Sign up and get your API key

# Gmail SMTP Setup (for sending emails):
# 1. Enable 2-Factor Authentication on your Google account
# 2. Go to https://myaccount.google.com/apppasswords
# 3. Create an app password for "Mail"
# 4. Use your Gmail address as SMTP_USERNAME
# 5. Use the generated app password as SMTP_PASSWORD
#
# Alternative email providers:
# - Outlook: smtp.office365.com:587
# - SendGrid: smtp.sendgrid.net:587
# - Mailgun: smtp.mailgun.org:587
